# 打开 Pokémon GO 整理助手

直接双击：

```text
release\PokemonGO-Renamer.exe
```

这是完整的中文桌面界面，不需要打开 PowerShell，不需要编辑 `.env`，也不需要复制提示词。

首次使用建议：

1. 等待 iPhone MCP、本地模型、执行引擎三个状态都变成绿色。
2. 先点击“只读预演”。
3. 确认识别正确后，把单批上限设为 `3`，点击“开始安全改名”。
4. 观察前三只的结果，稳定后再提高到 `20`。

“立即停止”会同时关闭本地模型任务和手机控制代理，写权限不会在后台保留。

